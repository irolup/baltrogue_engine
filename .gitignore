# Editor
.vscode/
*.swp

# Build outputs
/build/
/build_linux/
/build_editor/
/bin/
/obj/

# Generated config
imgui.ini

# Note: textures.txt, models.txt, scripts.txt, fonts.txt are generated but needed for VPK builds
# They are regenerated by the editor when building, but should be committed for initial builds

# Vendor dependencies (compiled binaries and source archives)
vendor/bullet/build/
vendor/bullet/build_vita/
vendor/bullet/build3/
vendor/bullet/lib/
vendor/bullet/lib_vita/
